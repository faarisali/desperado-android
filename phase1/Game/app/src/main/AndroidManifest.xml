<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LevelTwo.LevelTwoReplayActivity" />
        <activity android:name=".AbstractActivity" />
        <activity android:name=".WinActivity" />
        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoseActivity" />
        <activity
            android:name=".PauseScreen"
            android:theme="@style/Theme.Transparent" />
        <activity android:name=".MainMenu" />
        <activity android:name=".LevelOne.LevelOneActivity" />
        <activity android:name=".LevelThree.LevelThreeActivity" />
        <activity android:name=".LevelTwo.LevelTwoActivity" />
    </application>

</manifest>